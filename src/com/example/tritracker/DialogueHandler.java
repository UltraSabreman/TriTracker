package com.example.tritracker;


public class DialogueHandler {
	/*public interface onGoodClick {
		public String name = "Ok";
		public void run(); 
	}
	public interface onBadClick {
		public String name = "Cancel";
		public void run(); 
	}
	
	private onGoodClick ok = null;
	private onBadClick cancel = null;
	private Activity activity = null;
			
	public DialogueHandler(Activity act) {
		this.activity = act;
	}
	public DialogueHandler(Activity act, onGoodClick ok, onBadClick cancel) {
		this.ok = ok;
		this.cancel = cancel;
		this.activity = act;
	}
	
	
	public void createDialog(String body) {
		build(null, body, null);
	}
	
	public void createDialog(String title, String body) {
		build(title, body, null);
	}
	
	public void createDialog(String title, String body, View customView) {
		build(title, body, customView);
	}
	
	private void build(String title, String body, View customView) {
		AlertDialog.Builder builder  = new AlertDialog.Builder(activity);
		
		if (customView != null)
			builder.setView(customView);
		if (title != null)
			builder.setTitle(title);
		if (body != null)
			builder.setTitle(body);
		
		if (ok != null) {
			builder.
		}
		
	}*/
	
	
}
